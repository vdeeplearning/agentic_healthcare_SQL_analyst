# Contributing

Create a branch, add tests for behavior changes, run `python -m pytest`, and open a focused pull request. Never contribute real patient data, secrets, generated databases, or logs. Security issues should be reported privately rather than filed publicly. Suggested issue templates: bug report, safeguard bypass, metric proposal, and reference-query contribution.

