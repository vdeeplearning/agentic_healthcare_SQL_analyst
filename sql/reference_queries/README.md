# Reference SQL curriculum

Each numbered query is executable against the generated SQLite database. The header records the question, expected shape, assumptions, and learning concepts. Queries deliberately progress from basic reporting to cohort logic, windows, anomaly detection, statistical extraction, and performance analysis.

